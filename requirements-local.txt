# =============================================
# PlotPilot - Local model extended dependencies (~2GB)
# =============================================
#
# Usage: pip install -r requirements-local.txt
#
# Contains large dependencies for local Embedding models.
# Only install when you need to use "Local Vector Mode".
#
# Make sure you have at least 5GB free disk space before installing.
# First install may take 5-20 minutes depending on network speed.
# =============================================

# -- Numerical Computing --
numpy>=1.21.0

# -- Vector Index --
faiss-cpu>=1.7.4

# -- Local Embedding Models (includes PyTorch dependencies) --
sentence-transformers>=2.2.0
