# =============================================
# PlotPilot - Core dependencies (lightweight, fast install)
# =============================================
#
# Usage: pip install -r requirements.txt
#
# Only includes minimal dependencies for core functionality.
# Local vector models (sentence-transformers / torch / faiss)
# have been moved to requirements-local.txt.
#
# After installing this file, you can start the service with OpenAI API mode.
# =============================================

# -- Web Framework --
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.6

# -- Data Validation --
pydantic>=2.0.0

# -- HTTP Client --
httpx==0.27.0

# -- AI / LLM SDK --
openai>=1.65.0,<2.0.0
anthropic>=0.40.0
volcengine-python-sdk[ark]>=1.0.0

# -- Utilities --
python-dotenv>=1.0.0
json-repair>=0.30.0

# ── 导出（DOCX / EPUB / PDF）──
python-docx>=1.1.0
ebooklib>=0.18
lxml>=4.9.0
fpdf2>=2.7.0

# ── 测试（开发用） ──
pytest>=7.0,<9.0
pytest-asyncio>=0.23,<1.0
